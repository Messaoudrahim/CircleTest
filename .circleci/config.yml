version: 2
jobs:
  build:
    docker:
      - image: ngix

    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world, success.

      - run:
          name: Print the Current Time
          command: date
